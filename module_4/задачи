🧩 Задача 1 — Проверка структуры ответа
📌 Условие:

Создай тест, который:

Делает POST /booking
Проверяет:
статус код = 200
в ответе есть:
bookingid
booking
внутри booking есть все поля:
firstname
lastname
totalprice
depositpaid
bookingdates

👉 Подсказка:

response.json()
🧩 Задача 2 — Проверка данных ответа
📌 Условие:

После создания брони:

Проверь, что:
firstname в ответе совпадает с отправленным
lastname совпадает
totalprice совпадает

👉 То есть:

assert response.json()["booking"]["firstname"] == "Jim"
🧩 Задача 3 — Использование фикстуры
📌 Условие:

Вынеси данные бронирования в фикстуру:

@pytest.fixture
def booking_data():
    return {...}

И используй её в тесте.

👉 Цель:
понять, как передавать данные в тесты

🧩 Задача 4 — Создание + получение (GET)
📌 Условие:
Создай бронь (POST)
Получи bookingid
Сделай GET /booking/{id}
Проверь:
статус = 200
данные совпадают с тем, что отправлял

👉 Тут уже связываешь два запроса

🧩 Задача 5 — Фикстура с зависимостью
📌 Условие:

Сделай две фикстуры:

@pytest.fixture
def booking_data():
    ...

@pytest.fixture
def create_booking(booking_data):
    ...

👉 create_booking должен:

делать POST
возвращать bookingid

Теперь тест использует create_booking

🧩 Задача 6 — Проверка удаления
📌 Условие:
Создай бронь
Удали её (DELETE /booking/{id})
Попробуй получить её снова

👉 Проверить:

статус НЕ 200 (например 404 или 405)
🧩 Задача 7 — Параметризация (🔥 важная)
📌 Условие:

Проверь создание брони с разными именами:

@pytest.mark.parametrize("name", ["Jim", "Anna", "John"])

👉 В тесте:

подставляй имя
проверяй, что оно сохранилось
🧩 Задача 8 — Негативный тест
📌 Условие:

Отправь некорректные данные:

{
    "firstname": 123
}

👉 Проверить:

сервер НЕ должен вернуть 200
(или проверить, как он себя ведёт)
🧩 Задача 9 — Scope (важная)
📌 Условие:

Сделай фикстуру:

@pytest.fixture(scope="module")
def booking_id():
    ...

👉 Она:

создаёт бронь один раз
возвращает id

Используй её в нескольких тестах:

один проверяет GET
другой проверяет структуру
🧩 Задача 10 — Мини-фреймворк (🔥🔥)
📌 Условие:

Сделай:

отдельную функцию или фикстуру для:
create_booking
get_booking
чтобы тесты выглядели так:
def test_booking_flow(create_booking):
    booking_id = create_booking
    response = get_booking(booking_id)
    assert response.status_code == 200
🚀 Если хочешь челлендж

👉 Попробуй:

объединить всё:
фикстуры
параметризацию
несколько запросов
проверки структуры
💡 Совет

Если будет сложно:

решай по одной задаче
можешь скидывать код — разберём и улучшим

Хочешь, я:
👉 проверю твоё решение
👉 или дам эталонное решение после того, как попробуешь сам

Как удобнее?